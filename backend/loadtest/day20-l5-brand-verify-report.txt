==========================================================================
L5① 验收：品牌字典（GET /api/brands + GET /api/admin/brands）
==========================================================================

[等待应用就绪] 127.0.0.1:8080 ...
  应用已就绪。
  库基线：brands 全部=7  status=1 的=7  max(id)=7
  ★ 已插入临时【停用】品牌 id=11（它只应出现在管理端，不应出现在 C 端）

[登录] admin(SUPER_ADMIN) / op_product(PRODUCT_ADMIN) / op_order(ORDER_ADMIN)
  admin:OK  op_product:OK  op_order:OK

--------------------------------------------------------------------------
A C 端 /api/brands（匿名，白名单精确路径）
--------------------------------------------------------------------------
  [PASS] A1 匿名 GET /api/brands → HTTP 200
  [PASS] A2 body.code == 200
  [PASS] A3 data 是【数组】（Result<List<BrandVO>>，不是分页对象）
  [PASS] A4 条数 == DB 中 status=1 的品牌数 (7)
  [PASS] A5 id 序列与 DB(status=1, order by id) 完全一致 [1, 2, 3, 4, 5, 6, 7]
  [PASS] A6 ★ 停用品牌（id=11）不在 C 端结果里
  [PASS] A7 name/logo/description 逐条 == DB（BeanUtils 拷字段没漏）

--------------------------------------------------------------------------
B ★白名单粒度三态：/api/brands 放行，/api/brands/999 与 POST 都必须被挡
--------------------------------------------------------------------------
  [PASS] B1 匿名 GET /api/admin/brands → 真 HTTP 401（未被白名单放行）
  [PASS] B2 匿名 POST /api/brands → 真 HTTP 401（白名单只放行 GET，是「路径+方法」粒度）
  [PASS] B3 ★ 匿名 GET /api/brands/999 → 真 HTTP 401 —— 若白名单写成 /api/brands/** 这里不会 401
  [PASS] B4 带 admin token 再打 /api/brands/999 → 404（证明这条路径确实没路由，B3 的 401 出自 Security）

--------------------------------------------------------------------------
C 管理端权限矩阵（★ 区分度靠「谁没有」）
--------------------------------------------------------------------------
  [PASS] C1 admin(SUPER_ADMIN) → HTTP 200 + code 200
  [PASS] C2 admin 条数 == DB 全部品牌数 (8)（含停用）
  [PASS] C3 ★ admin 结果【包含】停用品牌 id=11（管理员要能看见它才能重新启用）
  [PASS] C4 op_product(PRODUCT_ADMIN，brand:list 已补发) → HTTP 200 + code 200
  [PASS] C5 ★ op_order(ORDER_ADMIN，无 brand:list) → 真 HTTP 403（不是 200+code）
  [PASS] C6 ★ admin 与 op_product 的响应体【逐字节相同】（结果与调用者无关）
  [PASS] C7 /api/admin/brands/999 带 admin → 404（管理端目前只有 list 一个口子，没有漏出的端点）

--------------------------------------------------------------------------
D ★两端口径差异 —— 与 L2 拆 getDetail/getAdminDetail 同构的证据
--------------------------------------------------------------------------
  [PASS] D1 管理端条数 - C 端条数 == 1（差值恰好是那一个停用品牌）
  [PASS] D2 管理端 id 集合 - C 端 id 集合 == {11}
  [PASS] D3 C 端 id 集合 ⊂ 管理端 id 集合（C 端是管理端的子集，不是另一份数据）

--------------------------------------------------------------------------
E 清理（临时品牌删掉，库回基线）
--------------------------------------------------------------------------
  [PASS] E1 清理后 brands 行数 == 基线 7
  [PASS] E2 ★ 高水位线：不存在 id > 基线 max(id)=7 的品牌
  [PASS] E3 清理后 C 端条数 == 基线 7（接口回到原状）

==========================================================================
TOTAL: PASS=24  FAIL=0   （期望断言数 24）
VERDICT: OK 全绿
==========================================================================
