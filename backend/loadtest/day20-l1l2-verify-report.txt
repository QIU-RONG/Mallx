==========================================================================
Day 20 补漏验收：L1 分页夹紧 + L2 详情判上架
==========================================================================

[等待应用就绪] 127.0.0.1:8080 ...
  应用已就绪。
      demo(C端)   : OK
      admin(超管) : OK
  库基线：products=5  product_skus=7  C端存活(status=1,is_deleted=0)=5

--------------------------------------------------------------------------
A 基线：默认参数下的列表（改动不得影响正常路径）
--------------------------------------------------------------------------
  [PASS] A1 GET /api/products → HTTP 200
  [PASS] A2 body.code == 200
  [PASS] A3 total == DB 存活商品数 (5)
  [PASS] A4 默认 size=10 时 records 条数 == total（5 条装得下）

--------------------------------------------------------------------------
B L1 ★ 分页夹紧（size=0 的区分度 / size=-1 的上限语义）
--------------------------------------------------------------------------
  [PASS] B1 ★ size=0 → records 恰好 1 条（未夹紧会是 0 条；夹紧后 Math.max(0,1)=1）
  [PASS] B2 size=0 时 total 仍 == DB 存活数（夹紧没改过滤口径）
  [PASS] B3 current=0 → HTTP 200 + code 200（page<1 被夹到 1，不报错）
  [PASS] B4 current=0 → records 非空（确实取的是第 1 页，不是空页）
  临时插入 150 条商品（category_id=1, name 前缀 L1-TEMP-）...
  [PASS] B5 造数生效：products == 基线 5 + 150
  [PASS] B6 ★ size=-1 → records 恰好 1 条（夹紧公式 min(max(size,1),100) 把负数夹到【下限 1】；★★ 未夹紧时 MP 的语义是「size<0 = 不限量」→ 会返回全部 155 条）
  [PASS] B7 size=-1 时 total == 155（total 不受夹紧影响，仍如实报总数）
  [PASS] B8 size=1000 → records 恰好 100 条（上限两侧都夹得住）
  [PASS] B9 size=1000 的 id 列表 == DB 中 id 最大的 100 个（ORDER BY id DESC 未被破坏）

--------------------------------------------------------------------------
C L1 ★ 只加夹紧、没动过滤口径（Day 19 链路 B 的对照组前提仍在）
--------------------------------------------------------------------------
  [PASS] C1 keyword=iphone → total == 0（like 仍大小写敏感）
  [PASS] C2 keyword=iPhone → total == 1（对照组的存在理由，未被顺手改掉）

--------------------------------------------------------------------------
D 高水位线清理（临时数据按 name 前缀删除）
--------------------------------------------------------------------------
  [PASS] D1 清理后 products == 基线 5
  [PASS] D2 残留临时商品 == 0 条

--------------------------------------------------------------------------
E L2 ★ C 端详情判上架（下架 vs 上架 / C 端 vs 管理端 反向对照）
--------------------------------------------------------------------------
  靶子商品 id=1
  [PASS] E1 靶子商品 DB status == 1（前提成立）
  [PASS] E2 上架时 C 端 GET /api/products/1 → code 200
  [PASS] E3 上架时管理端 GET /api/admin/products/1 → code 200
  [PASS] E4 管理端详情字段与 DB 一致（skus=3 / images=2 / name 对齐）
  [PASS] E5 下架：PUT /api/admin/products/1 {"status":0} → code 200
  [PASS] E6 DB 核对：status == 0（下架真的落库了）
  [PASS] E7 ★ 下架后 C 端详情 → code 404（伪装 404，不是 403）
  [PASS] E8 ★ 下架后管理端详情 → code 200 且 skus/images 仍完整（管理员还得能改价/重上架）
  [PASS] E9 ★★ 同一 id、同一时刻：两端结果必须【不同】（都 200=没拆开；都 404=砸了管理端）
  [PASS] E10 ★ 伪装 404 一致性：下架商品与「不存在的 id」的响应逐字段相同
  [PASS] E11 恢复：PUT {"status":1} → code 200
  [PASS] E12 DB 核对：status 回到 1
  [PASS] E13 恢复后 C 端详情 → code 200（商品重新可见）

--------------------------------------------------------------------------
F 零写入收尾（跑完一切回基线）
--------------------------------------------------------------------------
  [PASS] F1 products == 基线 5
  [PASS] F2 product_skus == 基线 7
  [PASS] F3 无残留脏状态（is_deleted<>0 或 status<>1 的行 == 0）

==========================================================================
TOTAL: PASS=33  FAIL=0   （期望断言数 33）
VERDICT: OK 全绿
==========================================================================
