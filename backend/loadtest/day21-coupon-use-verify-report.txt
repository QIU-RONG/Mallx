==============================================================================
Day 21 优惠券（阶段二）验收：A 不用券原路 / B 满减核销 / C 折扣与边界
                              D 归属(IDOR) / E 退券 / F 库存对账
==============================================================================

[等待应用就绪] 127.0.0.1:8080 ...
  应用已就绪。

------------------------------------------------------------------------------
[0] 夹具：临时用户 + 地址 + 商品 + 4 个价格受控的 SKU + 库存 + 券
------------------------------------------------------------------------------
  高水位线：orders > 9015 / inventory_logs > 0
  基线：orders=8  inventory_logs=0  coupons=0  user_coupons=0  users=2
  [PASS] 0.1 临时用户 d21tmp1 登录成功（password 列写 {noop} 明文，不必算 BCrypt）
  [PASS] 0.2 夹具就位：4 个临时 SKU（100.00/200.00/99.99/33.45）+ 库存 1000 + 11 张券 + 1 个地址

  临时数据：user=16  address=3  product=333  sku=[35, 36, 37, 38]  coupons=[456, 457, 458, 459, 460, 461, 462, 463, 464, 465, 466]
  user_coupons：[('C1', 19), ('C10', 29), ('C11', 28), ('C2', 20), ('C3', 21), ('C4', 22), ('C5', 23), ('C6', 24), ('C7', 25), ('C8', 26), ('C9', 27)]

------------------------------------------------------------------------------
A 组 · 不用券原路（★ 本日最高优先级：M1 跑的就是这条链路）
------------------------------------------------------------------------------
    A1 不用券下单                     coupon=None   sku=35 -> http=200 code=200 order=9054
  [PASS] A1 不带 userCouponId 下单 → code=200（老链路一个字节都没变）
  [PASS] A2 DB：discount_amount = 0.00（不是 null）且 pay == total == 100.00
  [PASS] A3 DB：total_amount == Σ(price×quantity)（服务端算钱，不信接口自报）
  [PASS] A4 不用券下单【完全没碰券】：status='USED' 的行数不变（0）
  [PASS] A5 ★ 恒等式全表成立：pay_amount = total_amount - discount_amount（违反 0 行）

------------------------------------------------------------------------------
B 组 · 满减券（FIXED 20 / 门槛 100）核销
------------------------------------------------------------------------------
    B1 用 C1 满减 20                coupon=19     sku=35 -> http=200 code=200 order=9055
  [PASS] B1 用满减券下单 → code=200
  [PASS] B2 DB：discount_amount=20.00 且 pay_amount=80.00（total − 20）
  [PASS] B3 券行 status='USED'
  [PASS] B4 券行 order_id = 新订单 id(9055)（不是留空、也不是别的单）
  [PASS] B5 券行 used_at 非空（自定义 XML 的 UPDATE 不走填充器，漏写就是 null）
    B6 同一张券再用                    coupon=19     sku=35 -> http=200 code=400 order=None
  [PASS] B6 同一张券再用 → code=400（被 markUsed 的 CAS 挡下）
  [PASS] B7 ★ 第 6 步失败 → 订单没建成（orders 数没涨）
  [PASS] B8 ★ 失败后被核销的那行没被改写：order_id 仍指向 B1 的订单

------------------------------------------------------------------------------
C 组 · 折扣券（DISCOUNT）与四条边界
------------------------------------------------------------------------------
    C1 rate=0.80                 coupon=22     sku=35 -> http=200 code=200 order=9056
  [PASS] C1 rate=0.80 + 总额 100.00 → code=200
  [PASS] C2 rate=0.80 → 抵扣 20.00（= total×(1−0.80)；把 0.80 读成 80 会算出负数）
    C3 封顶 300>200                coupon=20     sku=36 -> http=200 code=200 order=9057
  [PASS] C3 面额 300 > 总额 200 → code=200（V1.0 选封顶，不让下单失败）
  [PASS] C4 ★ 抵扣封顶到 total：discount=200.00 且 pay_amount=0.00（绝不为负）
    C5 舍入 33.45×0.10             coupon=23     sku=38 -> http=200 code=200 order=9058
  [PASS] C5 总额 33.45 + rate=0.90 → code=200
  [PASS] C6 ★ 抵扣 = 3.345 → 3.35（HALF_UP）而不是 3.34（BigDecimal 默认 HALF_EVEN）
    C7 脏券 rate=80                coupon=24     sku=35 -> http=200 code=400 order=None
  [PASS] C7 ★ rate=80 的脏券 → code=400「折扣率取值必须是 0 到 1 之间」（不许算出负数）
    C8 门槛差一分                     coupon=21     sku=37 -> http=200 code=400 order=None
  [PASS] C8 总额 99.99 < 门槛 100 → code=400（差一分也不能用）
    C9 已过期                       coupon=25     sku=35 -> http=200 code=400 order=None
  [PASS] C9 已过期的券 → code=400
    C10 尚未开始                     coupon=26     sku=35 -> http=200 code=400 order=None
  [PASS] C10 ★ 尚未开始的券 → code=400（★ 靶子是 start_time 在未来的券，库里现有券全在过去 ⇒ 不造数据这条恒真）
    C11 已下架                      coupon=27     sku=35 -> http=200 code=400 order=None
  [PASS] C11 券种已下架 → code=400
  [PASS] C12 ★ 上面 5 个失败用例【一个订单都没建成】（抛异常 ⇒ 事务整体回滚）

------------------------------------------------------------------------------
D 组 · 归属（IDOR）—— ★ 光看状态码什么也证明不了，必须去 DB 核
------------------------------------------------------------------------------
    D1 用别人的券                     coupon=29     sku=35 -> http=200 code=404 order=None
  [PASS] D1 用【别人】的 userCouponId 下单 → code=404（伪装 404，与不存在同码同文案）
  [PASS] D2 ★ DB：那张券【原封不动】（status/order_id/used_at 三样都没变）
  [PASS] D3 ★ HTTP 层是 200 —— 404 是【业务码】而不是协议码（本项目状态码混合约定；光看 HTTP 码会以为「下单成功」）
    D4 不存在的券                     coupon=99999999 sku=35 -> http=200 code=404 order=None
  [PASS] D4 不存在的 userCouponId → code=404
  [PASS] D5 ★★ 与 D1 的响应【逐字节相同】—— 这是「伪装 404」的判据：两者可区分就说明 IDOR 防线有裂缝
    D6 已核销的券（归属对、状态错）            coupon=19     sku=35 -> http=200 code=400 order=None
  [PASS] D6 ★ 反向对照：自己的【已核销】券 → code=400 而不是 404（证明 404 不是「什么都报 404」）

------------------------------------------------------------------------------
E 组 · 退券（逆向，本日含金量最高的一组）
------------------------------------------------------------------------------
    E1 用 C11 下单                  coupon=28     sku=35 -> http=200 code=200 order=9059
  [PASS] E1 用券下单 → code=200
  [PASS] E2 DB：下单即核销 —— status='USED' 且 order_id=9059
  [PASS] E3 取消该订单 → code=200
  [PASS] E4 ★ 退券后 status='UNUSED'
  [PASS] E5 ★ 且 order_id IS NULL（把旧订单号擦干净，不是只改状态）
  [PASS] E6 ★ 且 used_at IS NULL（三样缺一不算退干净）
    E7 退回来的券再用一次                 coupon=28     sku=35 -> http=200 code=200 order=9060
  [PASS] E7 ★ 退回来的券能再次使用 → code=200
  [PASS] E8 ★ 且 order_id 指向【新】订单 9060（不是旧订单 9059）
    （E7 前 USED 行数 = 4）
    E9 不用券下单（为退款对照）              coupon=None   sku=35 -> http=200 code=200 order=9061
  [PASS] E9 ★ 反向对照：不用券的订单取消 → code=200（退券 0 行是常态，不许当失败 —— 当成失败 M1 当场红）
  [PASS] E10 ★ 取消【没用券】的订单不会误动别人的券：USED 行数不变（5）
  [PASS] E11 同一张订单重复取消 → code=400（CAS 抢不到，且不会再退一次券）

------------------------------------------------------------------------------
F 组 · 库存对账（锁定量 = 未取消订单占用的件数）
------------------------------------------------------------------------------
  [PASS] F1 ★ 4 个临时 SKU 的库存守恒：available+locked+sold==total 且 total 未被改动
  [PASS] F2 ★ S1 的 locked_stock(4) == 未取消订单占用的件数(4) —— 取消要真的回补库存
  [PASS] F3 ★ 恒等式全表成立（收尾再断一次：整轮的写入都没破坏它）

------------------------------------------------------------------------------
G 组 · 清理（高水位线 + 前后缀，跑完回基线）
------------------------------------------------------------------------------
  [PASS] G1 临时券已删干净（残留 0 张）
  [PASS] G2 临时商品/SKU/库存已删干净（残留 SKU 0 个）
  [PASS] G3 临时用户/地址/购物车已删干净（残留用户 0 个）
  [PASS] G4 ★ 订单族回基线：orders=8、inventory_logs=0（都是本脚本开始时的值）
  [PASS] G5 券族回基线：coupons=0、user_coupons=0
  [PASS] G6 users 回基线 2（临时用户已删）

==============================================================================
TOTAL: PASS=53  FAIL=0   （期望断言数 53）
VERDICT: OK 全绿
==============================================================================
下一步：python day17-m1-regression.py --baseline → 三条 E2E → 不带参数再跑一次（期望 198/198）
