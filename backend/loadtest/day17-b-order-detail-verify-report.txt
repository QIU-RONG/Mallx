==============================================================================
Day 17 · 链路 B 验收 —— GET /api/admin/orders/{id}
==============================================================================
应用可达: 是（等待 0.0s）

[1] DB 基线
      orders 6 张；归属 user_id 集合 = ['1']；明细共 6 条
      逐单明细条数 = {1: 1, 2: 1, 3: 1, 4: 1, 5: 1, 6: 1}

[2] 登录  demo:OK  intruder:OK  admin:OK  op_order:OK
  ✅ 四个身份全部登录成功                                                 op_order token 可用于权限矩阵

[3] 判别力前提（实测，不假设）
      op_order 的 admins.id = 2 ；订单 6 的 user_id = 1
  ✅ op_order.id ≠ 该订单的 user_id（降级实现会被这条抓出来）                    2 vs 1

[4] 权限三态 —— /api/admin/orders/6
  ✅ 匿名（不带头）-> 真 HTTP 401                                       实得 401
  ✅ C 端 demo token -> 真 HTTP 403                               实得 403
  ✅ C 端 intruder token -> 真 HTTP 403                           实得 403

[5] ★★ 数据权限反转 —— 同一订单 id，四个身份 × 两个端点
  ✅ demo（属主）                C 端      -> HTTP 200 + code 200    实得 HTTP 200 + code 200
  ✅ intruder（非属主）          C 端       -> HTTP 200 + code 404    实得 HTTP 200 + code 404
  ✅ op_order（非属主管理员）    管理端          -> HTTP 200 + code 200    实得 HTTP 200 + code 200
  ✅ admin（超管）              管理端       -> HTTP 200 + code 200    实得 HTTP 200 + code 200

      ★ 两条关键推论：
  ✅ C 端非属主被伪装成 404（读越权防护）                                      code=404
  ✅ ★ 管理端【不筛归属】：非属主管理员拿到 200（不是 404）                           op_order code=200
  ✅ 管理端两身份返回的详情体逐字节相同（与调用者是谁无关）                                

[6] 防枚举 —— C 端「不是你的单」与「不存在的单」必须不可区分
  ✅ 两者 HTTP 200 + code 404                                     404 / 404
  ✅ 两者响应体逐字节相同（★ 否则可遍历 id 画出「哪些单真实存在」的地图）                      {"code": 404, "message": "订单不存在", "data": null} | {"code": 404, "message": "订单不存在", "data": null}

[7] ★ 逐单逐字段对账（管理端，14 个字段 + items 9 个字段）
  ✅ 每次返回的 key 集合 == VO 的 14 字段                                 []
  ✅ ★ VO 不含 userId（6 单全覆盖，key 集合里没有它）                          6 单
  ✅ 无 null 字段（未流转的时间戳除外）                                       []
  ✅ 订单头 13 个标量字段逐个与 DB 一致                                      []
  ✅ items 逐条逐字段与 DB 一致（含 9006 的 2 条明细）                         []
      覆盖订单 = [6, 5, 4, 3, 2, 1]

[8] 边界（HTTP 200 + body.code，不是真 404/400）
  ✅ 不存在的 id -> HTTP 200 + code 404                             HTTP 200 + code 404
  ✅ 非数字 id /abc -> HTTP 200 + code 400                         HTTP 200 + code 400

==============================================================================
VERDICT: OK —— 21 / 21 项全过
  复用路线成立：detail 与 detailByAdmin 共用 buildDetail；管理端不过滤归属；
  C 端防枚举不变；14+9 字段全部与 DB 对账通过。
==============================================================================
