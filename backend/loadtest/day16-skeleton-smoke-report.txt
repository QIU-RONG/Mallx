==============================================================================
Day 16 skeleton smoke test - is the scaffolding wired correctly?
target: http://127.0.0.1:8080
==============================================================================

[1] the application answers at all
  GET /api/hello -> http=200

[2] login (the token never touches disk)
  POST /api/auth/login -> http=200 code=200 token_len=191

[3] ★ mall-review is wired into mall-server (missing jar dependency = 404 here)
  GET /api/products/1/reviews   (NO token) -> http=200 code=200
      phase SKELETON -> code=500 expected (body is UnsupportedOperationException)
      phase DONE     -> code=200 expected
  GET /api/products/999999/reviews (NO token) -> http=200 code=200

[4] the two authenticated endpoints must reject anonymous callers
  POST /api/reviews   (NO token) -> http=401 {"code":401,"message":"未登录或登录已过期","data":null}
  GET  /api/reviews/my (NO token) -> http=401 {"code":401,"message":"未登录或登录已过期","data":null}
  ★ 说明：这两个 401 与白名单【无关】—— 它们在 anyRequest().authenticated() 的辖区内。
     商品列表那条才是白名单的功劳（同样匿名，却进得去）。

[5] with a token, the two authenticated endpoints are routed
  POST /api/reviews (demo token, bogus orderItemId) -> http=200 code=400
      skeleton -> 500 (UnsupportedOperationException)
      finished -> 404 = "订单明细不存在"  <- the fake 404 rides in body.code
  GET  /api/reviews/my (demo token) -> http=200 code=200

[6] ★ DTO validation is already live - it runs BEFORE the TODO body
  rating = 0      -> http=200 code=400 msg=评分不能低于 1
  rating = 6      -> http=200 code=400 msg=评分不能高于 5
  rating = null   -> http=200 code=400 msg=评分不能为空
  content 501ch   -> http=200 code=400 msg=评价内容最多 500 字
  no orderItemId  -> http=200 code=400 msg=订单明细 id 不能为空
  ★ 这一组在业务代码还没写的时候就能过 —— 因为注解挂在 DTO 上，@Valid 先跑。
    这正说明：参数校验与业务逻辑是两层，谁都不该越位。

[7] the two DDL constraints from 04-review-constraints.sql are in place
  order_item_id.is_nullable = NO   (must be NO)
  uk_reviews_order_item     = UNIQUE (order_item_id)
  reviews rows              = 0

==============================================================================
SKELETON CHECKS: 17 / 17 passed
==============================================================================
