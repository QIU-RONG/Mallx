# --- Java / Maven ---
**/target/
*.class
*.jar
!.mvn/wrapper/maven-wrapper.jar

# --- IDE ---
.idea/
*.iml
.vscode/
.settings/
.classpath
.project

# --- 系统 / 日志 ---
.DS_Store
Thumbs.db
*.log
logs/

# --- MallX 项目内部 ---
.workbuddy/
# AI 工作区（记忆/技能/临时数据），与本机绑定，不入库
.workbuddy-ai/
backend/_scaffold_backup_*/

# --- 工具临时文件 ---
.omo/
.tmp-zen-models.json

# --- Python（backend/loadtest 下压测脚本的字节码缓存） ---
__pycache__/
*.pyc

# --- 排查用临时产物（AI/手工调试生成，不入库） ---
/_*.txt
/build-check.txt
/javap-out.txt

# --- 本地学习/练习产物（非项目代码） ---
/learning/

# --- 部署密钥 ---
# deploy/.env 由 deploy/.env.example 复制而来，里面是【真实】的 JWT_SECRET 与 DB_PASSWORD。
# ★ .env.example 是模板、要入库；.env 是密钥、绝不入库。
#   （2026-09-25 订正：.env.example 里早就写着「.env 已在 .gitignore 排除」，
#     但这条规则当时并不存在 —— 属于「注释描述了一个不存在的机制」，最坏的一类文档漂移。）
/deploy/.env

# --- 与本项目无关的本地目录 ---
/dsh-skin-v2/
/dsh-themes/
